<section id="image-index">
  <div class="">
    <%= link_to "Back", new_user_concert_path(current_user) %>
  </div>
  <div class="">
    <% @images.each do |image| %>
    <div id="images-area">
      <section>
        <%= image_tag image.image.url unless image.image.url.nil? %>
        <%= image.name %>
      </section>
    </div>
    <% end %>
    <%= will_paginate @images %>
  </div>
</section>
